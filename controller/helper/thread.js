const createThread = ({ name, image, faultDescription, description }) => {};

const updateThread = ({
  name,
  image,
  faultDescription,
  description,
  userId,
  threadId,
}) => {};

const deleteThread = (threadId, userId) => {};
